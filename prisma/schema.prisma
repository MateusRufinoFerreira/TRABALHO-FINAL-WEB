// Plataforma de Gerenciamento de Avaliacoes (Provius)
//
// A URL de conexao nao fica neste arquivo: no Prisma 7 ela e definida em
// prisma.config.ts, que le DATABASE_URL do .env.

datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Usuario {
  id        String   @id @default(uuid())
  nome      String
  email     String   @unique
  senha     String // hash bcrypt
  createdAt DateTime @default(now())

  turmas Turma[]
}

model Turma {
  id        String   @id @default(uuid())
  nome      String
  codigo    String
  semestre  String
  createdAt DateTime @default(now())

  usuarioId String
  professor Usuario @relation(fields: [usuarioId], references: [id], onDelete: Cascade)

  alunos     Aluno[]
  avaliacoes Avaliacao[]
}

model Aluno {
  id        String   @id @default(uuid())
  nome      String
  matricula String   @unique
  email     String
  createdAt DateTime @default(now())

  turmas     Turma[]
  avaliacoes Avaliacao[]
}

model BancoQuestao {
  id        String   @id @default(uuid())
  titulo    String
  createdAt DateTime @default(now())

  questoes Questao[]
}

model Questao {
  id        String   @id @default(uuid())
  tipo      String // DISCURSIVA | MULTIPLA_ESCOLHA
  enunciado String
  peso      Float    @default(1.0)
  gabarito  String
  createdAt DateTime @default(now())

  bancoId String
  banco   BancoQuestao @relation(fields: [bancoId], references: [id], onDelete: Cascade)

  avaliacoes Avaliacao[]
}

model Avaliacao {
  id             String   @id @default(uuid())
  titulo         String
  tipo           String // PROVA | LISTA
  dataInicio     DateTime
  dataTermino    DateTime
  // A relacao N:M com Questao e implicita e sua tabela de juncao guarda apenas
  // as duas chaves estrangeiras, sem coluna de posicao. Por isso a ordem fixa
  // corresponde a ordem de criacao e o embaralhamento e feito no backend.
  ordemAleatoria Boolean  @default(false)
  createdAt      DateTime @default(now())

  turmaId String
  turma   Turma  @relation(fields: [turmaId], references: [id], onDelete: Cascade)

  alunos   Aluno[]
  questoes Questao[]
}
