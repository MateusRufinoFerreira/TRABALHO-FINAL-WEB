# Copie para .env e preencha os valores.

# Caminho do banco SQLite (relativo à raiz do projeto).
DATABASE_URL="file:./dev.db"

# Chave usada para assinar/verificar os tokens JWT (Aula 17).
# Use um valor longo e aleatório. NUNCA versione o .env real.
JWT_SECRET=""
